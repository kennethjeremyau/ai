# <Product> User Guide

## Summary

<One paragraph, plain language: what the product is for and what the reader gets out of it.>

**Who this guide is for:** <role or job, and what they are assumed to already know.>

## Prerequisites

You need:

- <account, license, or permission>
- <operating system, shell, or network access>
- <information to have on hand, such as a site ID or a colleague's email>

## Installation

1. <step>
2. <step>
3. <step>

You are ready when <observable sign that the install worked>.

## Operation

<The general form of the command, and anything that applies to all of it, such as
where the reader runs it from.>

```
<product> <command> [options]
```

| Command | What it does |
| --- | --- |
| `<command>` | <one line, starting with a verb> |
| `<command>` | <one line, starting with a verb> |

These options work with every command:

| Option | What it does |
| --- | --- |
| `<-x>`, `<--flag>` | <effect, and the default when the reader leaves it out> |

### <command>

<One or two sentences: what the command does and when the reader would run it.>

```
<the command, with the options a reader actually types>
```

<What the reader sees when it works.>

| Parameter | Required | What it does |
| --- | --- | --- |
| `<argument>` | Yes | <what the reader puts here, and what values are allowed> |
| `<-x>`, `<--flag>` | No | <effect, and the default when the reader leaves it out> |

### <Next command>

<Repeat the pattern above. One heading per command, in the order a new user meets
them.>

## Troubleshooting

### <The problem as the reader would say it, in one short sentence>

<Why it happens, in plain language.>

<What to do about it. Use numbered steps if there is more than one action.>

### <Next problem>

<Repeat the pattern above. One heading per problem.>

## Glossary

**<Term>.** <Plain-language definition.>

